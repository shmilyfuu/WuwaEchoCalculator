# WuwaEchoCalculator

WuwaEchoCalculator 是一个本地运行的鸣潮声骸截图识别与活动计分工具。应用使用 Windows WebView2 独立窗口，内置 PP-OCRv5 模型，图片只在本机完成识别。

可以自定义评分规则，最终合成一张声骸评分图。当前暂时只内置了一种活动评分规则。

当前版本：v1.2.2。

## 功能

- 支持点击、拖拽和 `Ctrl+V` 粘贴声骸截图。
- 本地识别五条辅音属性，并按合法档位进行校验。
- 支持手动修正属性和档位。
- 记录五件声骸，显示单项分数、小计、总分和平均分。
- 支持编辑、删除、覆盖、清空和导出 PNG。
- 支持停止当前识别并重新初始化 OCR。
- 数据保存在程序目录下的本地 WebView2 数据目录中。

## 最新更新

### v1.2.2

- 导出记录时改用 Windows 原生“另存为”窗口，可自行选择保存位置和文件名。
- 隐藏 WebView2 默认下载浮层，并在底部状态栏显示导出成功、取消或失败状态。
- 恢复导出标题输入框的文本选择、复制、粘贴和右键编辑菜单。
- 统一按钮的普通、悬停和按下状态配色，按钮不再改变鼠标指针形态。

<details>
<summary>历史更新</summary>

### v1.2.1

- 客户区域固定为 `1188 × 772`，保留 Windows 原生标题栏、最小化和关闭按钮。
- 主界面、空卡、确认弹窗和导出记录图按设计稿统一布局与配色。
- 清空、覆盖和删除记录统一使用二次确认弹窗。
- 支持停止当前识别，并修正图片拖拽和完整比例预览。
- 新增五件声骸记录图导出、动态总分和平均分。
- 修正 Windows 辅助功能文字缩放导致的界面裁切。

</details>

## 构建

网页资源使用 Vite 构建，Windows 宿主使用 C++17、Win32 和 Microsoft Edge WebView2 SDK。正式便携包由 GitHub Actions 的 `build.yml` 生成。
